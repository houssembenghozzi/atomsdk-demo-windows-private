<Window x:Class="Atom.VPN.Demo.CustomMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Email Sent" 
        Height="160" 
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White"
        WindowStyle="ToolWindow"
        Topmost="True">
    
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Information Icon -->
        <Ellipse Width="32" Height="32" Fill="#0078D7" Grid.RowSpan="2" Margin="10,0,10,0" VerticalAlignment="Top"/>
        <TextBlock Text="i" FontWeight="Bold" FontSize="22" Foreground="White" Grid.RowSpan="2"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="16,2,0,0"/>
        
        <!-- Message -->
        <TextBlock x:Name="MessageText" 
                  Grid.Column="1" 
                  Grid.Row="0" 
                  Grid.RowSpan="2" 
                  Text="A password reset link has been sent to email@example.com." 
                  TextWrapping="Wrap" 
                  VerticalAlignment="Center" 
                  Margin="10,10,10,10"/>
        
        <!-- OK Button -->
        <Button Grid.Row="2" 
                Grid.Column="1" 
                Content="OK" 
                Width="75" 
                Height="23" 
                HorizontalAlignment="Right" 
                Margin="0,10,10,0" 
                Click="OkButton_Click"/>
    </Grid>
</Window> 